# 晨光小鸡舍 (Tidegrain Bay) 游戏机制 & UI/UX 设计文档

> 用于其他Agent参考构建类似项目

## 项目概述

| 属性 | 值 |
|------|-----|
| 游戏类型 | 俯视角鸡舍模拟经营游戏 |
| 核心玩法 | 90秒内完成喂食、抚摸、拾蛋任务，追求S级评分 |
| 目标平台 | 手机 + PC 浏览器 |
| 技术栈 | HTML5 + CSS3 + JavaScript + Three.js (3D渲染) |
| 分辨率 | 360x640 (移动端优化) |
| 存储 | localStorage |

---

## 一、游戏架构

### 1.1 项目目录结构

```
project/
├── index.html              # 入口HTML
├── css/
│   └── style.css           # 所有样式
└── js/
    ├── main.js             # 主循环 (requestAnimationFrame)
    ├── config.js           # 常量配置
    ├── state.js            # 全局状态 (唯一数据源)
    ├── game-logic.js       # 游戏逻辑核心
    ├── screens.js          # 屏幕渲染 (HTML覆盖层)
    ├── ui.js               # UI组件 (按钮/进度条等)
    ├── input.js            # 鼠标/触摸输入处理
    ├── sensors.js         # 陀螺仪/设备方向
    ├── persistence.js     # 存档加载/保存
    ├── auth.js             # 账号系统
    ├── math.js             # 坐标投影/向量计算
    ├── assets.js           # 资源加载
    ├── render-bg.js        # 背景渲染
    ├── render-objects.js   # 3D物品渲染
    └── particles.js        # 粒子效果系统
```

### 1.2 模块职责

| 模块 | 职责 | 关键API |
|------|------|---------|
| `state.js` | 全局状态管理 | `State`, `loadSave()`, `saveSave()` |
| `game-logic.js` | 游戏逻辑 | `startGame()`, `interact()`, `endGame()` |
| `main.js` | 主循环/初始化 | `gameLoop()`, `init()` |
| `screens.js` | 屏幕切换 | `showScreen()`, `screenUpdate()` |
| `input.js` | 输入处理 | `onPointerDown()`, `onPointerMove()` |

### 1.3 屏幕流转

```
boot → auth → slots → menu
                  ↓
            permission → calibrate → playing
                  ↓              ↓
            shop ← ← ← ← ← ← ← ← ┘
                  ↓
              nextday → menu
```

**屏幕类型:**
- `boot` - 资源加载
- `auth` - 登录/注册
- `slots` - 存档选择
- `menu` - 主菜单
- `permission` - 权限申请(陀螺仪)
- `calibrate` - 陀螺仪校准
- `playing` - 游戏进行中
- `nextday` - 结算画面

---

## 二、游戏机制

### 2.1 核心循环（每日任务）

玩家在90秒倒计时内完成以下任务：

| 任务类型 | 目标数量 | 完成效果 |
|---------|---------|---------|
| 喂干草 | 填充所有食槽 | 小鸡好感+8 |
| 摸摸鸡 | 抚摸所有小鸡 | 小鸡好感+15 + 心形粒子 |
| 拾鸡蛋 | 拾取所有鸡蛋 | 获得金币奖励 |

### 2.2 评级系统

| 评级 | 条件 |
|------|------|
| S级 | 完成全部8项任务 + 用时≥35秒 |
| A级 | 完成全部8项任务 |
| B级 | 完成≥6项任务 |
| C级 | 完成≥3项任务 |
| D级 | 完成<3项任务 |

### 2.3 小鸡系统

**小鸡品质:**
```javascript
CHICKEN_TIERS = {
  yellow: { name: "小黄", color: "#ffd85e", price: 200 },
  white:  { name: "银羽", color: "#fff6d7", price: 400 },
  brown:  { name: "金栗", color: "#c98242", price: 600 }
}
```

**好感度系统:**
- 范围: 0-1000
- 每200点 = 1颗心（显示上限5颗）
- 摸头 +15好感，喂食 +8好感

**鸡蛋品质概率:**
```
bonus = tierBonus(0~0.22) + friendBonus(0~0.5) + heatBonus(0.12)

稀有蛋: bonus * 0.45  (价值100金币)
金星蛋: bonus * 0.70  (价值50金币)
银星蛋: bonus * 0.90  (价值20金币)
普通蛋: 剩余概率     (价值10金币)
```

### 2.4 交互流程

```
点击3D场景 → findObjectAtScreen(tx,ty) → 获取目标物体
                                        ↓
                          ┌─────────────┴─────────────┐
                          ↓                           ↓
                    手持干草?                      手持空?
                          ↓                           ↓
              点击食槽→放干草                 点击料斗→拿干草
                          ↓                           ↓
                    小鸡跑来吃                     状态更新
                          ↓                           ↓
                    任务完成                       HUD更新
```

**交互优先级:**
1. 如果持有干草且点击空食槽 → 放干草
2. 如果没持有且点击料斗 → 拿干草
3. 如果点击小鸡 → 抚摸
4. 如果点击鸡蛋 → 拾取

---

## 三、UI/UX设计

### 3.1 HUD布局 (360x640)

```
┌────────────────────────────────────┐
│ [干草 0/3] [摸摸 0/3] [鸡蛋 0/2]   │  ← 顶部任务栏 (10, 10)
│ [料斗 3]    ○○○       [90s]       │  ← 底部物品栏 (186, 74)
│                                    │
│           [指引箭头]               │  ← 动态指引 (超出视野时)
│                                    │
│                                    │
│        3D 鸡舍场景                  │  ← Three.js Canvas
│     (小鸡/料斗/鸡蛋)               │
│                                    │
│                                    │
│ [暂停][商店][状态][保存][结束][退出] │  ← 底部控制栏 (10, 584)
└────────────────────────────────────┘
```

### 3.2 屏幕层级

```
┌────────────────────────────────────┐
│           3D Canvas                │  ← 底层: Three.js渲染
├────────────────────────────────────┤
│           HTML Overlay             │  ← 中层: 游戏HUD
├────────────────────────────────────┤
│         Modal/Panel               │  ← 顶层: 暂停/商店/结算
└────────────────────────────────────┘
```

### 3.3 视觉规范

| 属性 | 值 |
|------|-----|
| 主色 | `#2e241d` (深棕色背景) |
| 强调色 | `#fcd264` (金色高亮) |
| 成功色 | `#4ade80` (绿色) |
| 错误色 | `#f87171` (红色) |
| 文字色 | `#fef3c7` (米黄) |
| 圆角 | 12px (按钮), 16px (面板) |
| 投影 | `0 4px 20px rgba(0,0,0,0.3)` |

### 3.4 粒子效果

| 效果 | 触发 | 表现 |
|------|------|------|
| 心形 ♥ | 抚摸小鸡 | 5-8个粉心向上飘散 |
| 闪光 ✨ | 拾取鸡蛋 | 金色光点向外扩散 |
| 浮动文字 | 获得金币 | "+10" 向上飘动消失 |

### 3.5 动画状态

| 状态 | 适用对象 | 表现 |
|------|---------|------|
| `idle` | 小鸡 | 轻微上下浮动 (sin波) |
| `walk` | 小鸡 | 位移到食槽/离开 |
| `peck` | 小鸡 | 低头啄食 |
| `happy` | 小鸡 | 抚摸后开心抖动 |
| `spin` | 鸡蛋 | 缓慢旋转 |

---

## 四、数据结构

### 4.1 全局State对象

```javascript
// state.js
const State = {
  // === 运行时不序列化 ===
  screen: "boot",           // 当前屏幕
  holding: null,            // 手持物品 ("hay" | null)
  hayLeft: 3,               // 料斗剩余干草
  feedPlaced: 0,            // 已放干草数
  eggsCollected: 0,         // 已拾取鸡蛋数
  petsDone: 0,              // 已摸鸡数
  timeLeft: 90,             // 剩余秒数
  paused: false,            // 暂停标志
  shopOverlay: false,       // 商店叠加层
  statusPanel: false,       // 状态面板

  // === 存档数据 (persistence.js) ===
  save: {
    days: 0,                // 游玩天数
    eggs: 0,                // 累计获得鸡蛋
    coins: 0,               // 金币余额
    bestClear: 0,           // 最佳通关时间(秒)
    perfectDays: 0,         // 完美通关天数

    // 小鸡数据: { [chickenId]: { tier, friendship, hunger } }
    friendship: {},
    chickenHunger: {},
    ownedChickens: {},

    // 鸡蛋统计
    eggQuality: {
      normal: 0,
      silver: 0,
      gold: 0,
      rare: 0
    },

    // 物品
    hayStorage: 10,         // 仓库干草
    heatLamp: false,         // 保温灯

    // 系统
    users: {},               // 账号: { username: password }
    slots: []                // 存档槽: [{ days, coins, chickens... }]
  }
};
```

### 4.2 存档键名

```javascript
// persistence.js
const SAVE_KEY = "morning-coop-save-v1";
const MAX_SLOTS = 3;
```

### 4.3 3D物体数据结构

```javascript
// render-objects.js
const objects = {
  hopper: { pos: vec3(0, 0, -2), radius: 0.8 },
  troughs: [
    { pos: vec3(-1.5, 0, -1), filled: false },
    { pos: vec3(0, 0, -1), filled: false },
    { pos: vec3(1.5, 0, -1), filled: false }
  ],
  chickens: [
    { id: "chicken_1", pos: vec3(...), tier: "yellow" },
    // ...
  ],
  eggs: [
    { id: "egg_1", pos: vec3(...), quality: "normal" }
  ]
};
```

---

## 五、核心函数参考

### 5.1 游戏逻辑

```javascript
// game-logic.js

/**
 * 开始新游戏
 */
function startGame() {
  State.screen = "playing";
  State.timeLeft = 90;
  State.holding = null;
  State.hayLeft = 3;
  State.feedPlaced = 0;
  State.eggsCollected = 0;
  State.petsDone = 0;
  // 初始化小鸡位置、鸡蛋等
}

/**
 * 处理点击交互
 * @param {number} screenX - 屏幕X坐标
 * @param {number} screenY - 屏幕Y坐标
 */
function interact(screenX, screenY) {
  const obj = findObjectAtScreen(screenX, screenY);
  if (!obj) return;

  if (State.holding === "hay" && obj.type === "trough" && !obj.filled) {
    // 放干草
    State.holding = null;
    obj.filled = true;
    State.feedPlaced++;
    spawnParticles("hay", obj.pos);
  } else if (State.holding === null && obj.type === "hopper") {
    // 拿干草
    State.holding = "hay";
    State.hayLeft--;
  } else if (obj.type === "chicken") {
    // 摸鸡
    State.petsDone++;
    State.save.friendship[obj.id] += 15;
    spawnParticles("heart", obj.pos);
  } else if (obj.type === "egg") {
    // 拾蛋
    const value = EGG_VALUES[obj.quality];
    State.save.coins += value;
    State.eggsCollected++;
    removeObject(obj.id);
    spawnParticles("sparkle", obj.pos);
  }
}

/**
 * 结束游戏
 * @param {boolean} auto - 是否超时自动结束
 */
function endGame(auto) {
  State.paused = true;
  const score = calculateScore();
  showScreen("nextday");
}
```

### 5.2 屏幕渲染

```javascript
// screens.js

const screens = {
  boot: () => { /* 加载资源 */ },
  auth: () => { /* 登录界面 */ },
  menu: () => { /* 主菜单 */ },
  playing: () => { /* 游戏HUD */ },
  nextday: () => { /* 结算 */ }
};

function showScreen(name) {
  // 隐藏所有屏幕
  document.querySelectorAll('.screen').forEach(s => s.style.display = 'none');
  // 显示目标屏幕
  if (screens[name]) screens[name]();
  State.screen = name;
}
```

### 5.3 坐标投影

```javascript
// math.js

/**
 * 3D坐标转屏幕坐标
 * @param {vec3} pos - 3D位置
 * @returns {vec2} 屏幕坐标
 */
function project(pos) {
  const vec = new THREE.Vector3(pos.x, pos.y, pos.z);
  vec.project(camera);
  return {
    x: (vec.x + 1) / 2 * canvas.width,
    y: (-vec.y + 1) / 2 * canvas.height
  };
}

/**
 * 屏幕坐标转3D射线
 */
function screenToRay(screenX, screenY) {
  const mouse = new THREE.Vector2(
    (screenX / canvas.width) * 2 - 1,
    -(screenY / canvas.height) * 2 + 1
  );
  raycaster.setFromCamera(mouse, camera);
  return raycaster.ray;
}
```

---

## 六、输入系统

### 6.1 视角控制

```javascript
// sensors.js

let yaw = 0;      // 水平角度
let pitch = 0;    // 垂直角度

function updateLook(yawDelta, pitchDelta) {
  yaw += yawDelta * 0.1;
  pitch = clamp(pitch + pitchDelta * 0.1, -55, 55);
  camera.rotation.set(pitch * DEG2RAD, yaw * DEG2RAD, 0);
}
```

### 6.2 触摸/鼠标处理

```javascript
// input.js

canvas.addEventListener('pointerdown', (e) => {
  if (State.screen !== 'playing' || State.paused) return;
  interact(e.clientX, e.clientY);
});

let isDragging = false;
let lastX, lastY;

canvas.addEventListener('pointermove', (e) => {
  if (!isDragging) return;
  const deltaX = e.clientX - lastX;
  const deltaY = e.clientY - lastY;
  updateLook(deltaX, deltaY);
  lastX = e.clientX;
  lastY = e.clientY;
});
```

---

## 七、存档系统

```javascript
// persistence.js

function loadSave() {
  try {
    const data = localStorage.getItem(SAVE_KEY);
    if (data) {
      State.save = JSON.parse(data);
    }
  } catch (e) {
    console.warn('存档加载失败', e);
  }
}

function saveSave() {
  try {
    // 只保存save对象,不保存运行时状态
    const { screen, holding, ...runtime } = State;
    localStorage.setItem(SAVE_KEY, JSON.stringify(State.save));
  } catch (e) {
    console.warn('存档保存失败', e);
  }
}
```

---

## 八、代码规范

### 8.1 文件组织

- 每个模块一个文件,集中管理相关功能
- 避免跨文件相互调用,通過State中转
- 屏幕渲染用HTML/CSS,不用Canvas

### 8.2 命名约定

| 类型 | 约定 | 示例 |
|------|------|------|
| 变量 | 小驼峰 | `hayLeft`, `feedPlaced` |
| 常量 | 全大写下划线 | `MAX_SLOTS`, `EGG_VALUES` |
| 函数 | 小驼峰 | `startGame()`, `interact()` |
| DOM类 | 中划线 | `screen-boot`, `btn-start` |
| CSS类 | 小驼峰 | `taskBar`, `controlPanel` |

### 8.3 性能优化

- 使用 `requestAnimationFrame` 而不是 `setInterval`
- 粒子池复用,避免频繁创建/销毁
- 3D物体不随视角更新,只在交互时检查
- 存档只在必要时保存(退出/切换槽)

---

## 九、构建清单

新建项目时按以下顺序实现:

1. **基础结构** - `index.html`, `css/style.css`, 基础JS框架
2. **状态管理** - `state.js` 全局State对象
3. **屏幕系统** - `screens.js` 屏幕切换
4. **3D渲染** - Three.js场景搭建,物体渲染
5. **输入处理** - `input.js` 点击/拖动
6. **游戏逻辑** - `game-logic.js` 交互核心
7. **UI组件** - `ui.js` HUD元素
8. **陀螺仪** - `sensors.js` 设备方向
9. **存档** - `persistence.js` localStorage
10. **粒子效果** - `particles.js`

---

## 十、调试技巧

```javascript
// 快速重置游戏
function resetGame() {
  localStorage.clear();
  location.reload();
}

// 查看当前状态
function dumpState() {
  console.log(JSON.stringify(State, null, 2));
}

// 跳过教程直接进入游戏
function quickStart() {
  loadSave();
  showScreen('playing');
  startGame();
}
```

---

## 十一、版本迭代优化建议

### 11.1 迭代目标

当前版本已经验证了“单场景 + 定点观察 + 轻操作 + 短回合反馈”是成立的。下一阶段不应继续只堆功能，而应把产品目标升级为:

1. 从“鸡舍原型”升级为“多场景日常循环原型”。
2. 从“能完成任务”升级为“进入场景就知道该做什么、做完会有情绪回报和资源回报”。
3. 从“单点交互”升级为“场景故事 + 动态反馈 + 经济流转”的轻经营体验。

建议后续所有场景都遵循统一的设计公式:

`场景 = 事件核心 + 动态元素 + 静态道具 + 完成反馈 + 资源出口`

其中:

- `事件核心` 决定玩家在这个场景里的主目标是什么。
- `动态元素` 负责提供生命感、变化感、观察乐趣。
- `静态道具` 负责锚定交互入口、强化空间叙事、承接资源流转。
- `完成反馈` 负责让玩家知道“我做对了，而且值得继续做”。
- `资源出口` 负责让每个场景产出的物品最终进入市场、订单或升级系统。

建议用以下体验指标约束设计:

- 玩家进入场景 `3秒内` 看懂主要目标。
- 玩家进入场景 `10秒内` 触发第一次有效反馈。
- 每个场景 `60-90秒` 完成一次完整循环。
- 每个场景至少有 `1个核心动态元素 + 3个静态交互点 + 1个可带走资源`。

### 11.2 UI 和交互逻辑 UX 优化

#### A. HUD 信息层级重构

当前 HUD 已经具备基本信息展示能力，但下一版应进一步压缩认知负担:

| 区域 | 当前状态 | 迭代建议 | 目标 |
|------|------|------|------|
| 顶部信息条 | 场景、Day、时间、体力并列展示 | 保留场景名/时间/体力，弱化次级说明文字 | 先让玩家看到“我在哪、还剩多久、还能做多少” |
| 任务进度条 | 以纯文本汇总 | 改为“图标 + 数值 + 完成态变色” | 降低阅读成本，提升扫视效率 |
| 底部按钮区 | 固定按钮文案偏多 | 改为“核心按钮固定 + 次级功能收纳进二级面板” | 减少移动端误触 |
| 中央准星 | 只承担瞄准功能 | 增加状态色变化，如可交互/已完成/无效交互 | 让玩家不用读字也知道当前能不能操作 |

#### B. 目标引导改为“上下文提示 + 世界内反馈”

当前提示主要依赖顶部文字，下一版建议改为三层引导:

1. `一级提示`: 顶部一句话任务提示，只说当前最重要的一件事。
2. `二级提示`: 场景内高亮、描边、闪点、边缘箭头，提示目标在何处。
3. `三级提示`: 若玩家 `4-6秒` 无有效操作，自动给出更明确的动作提示，例如“先去料斗拿干草”“海面右侧有鱼群翻花”。

这类做法适合当前项目的轻量移动端体验，也和 `Cozy Grove` 这类以日常节奏和清晰任务推进为核心的产品方向相符。

#### C. 交互反馈需要更明确地分成“拿起 / 放下 / 成功 / 失误”

建议所有场景统一反馈语言:

- `拿起`：物品徽章浮现，准星闪一下，轻音效。
- `放下`：目标物高亮熄灭，进度立即 +1。
- `成功`：粒子、浮字、音色更亮，同时刷新任务计数。
- `失误`：不要只弹 toast，最好附带轻微摇动、灰态准星或短促低音提示。

交互状态越统一，后面扩展码头、果园、集市时越不需要重新教育玩家。

#### D. 场景切换和结算应更像“日常流程”，而不是“关卡跳转”

建议每个场景结束后展示三类回报:

- `情绪回报`: 例如“鸡更亲人了”“今天的收获很顺手”。
- `资源回报`: 鸡蛋、鱼、蜂蜜、果酱、金币、声望。
- `世界回报`: 市场订单推进、摊位补货、次日事件解锁。

这类“采集/经营双循环”是 `DAVE THE DIVER` 一类产品最值得借鉴的地方: 前半段负责观察与采集，后半段负责整理、出售、升级，两个阶段相互喂养。

#### E. 移动端交互容错

建议加入以下细节:

- 点击判定允许轻微误差，避免必须精准点中小目标。
- 同类目标支持“就近吸附”，例如看向鸡身附近也能判定为抚摸。
- 错误交互优先给“纠错提示”，不要直接吞操作。
- 长文案不出现在主要操作热区，避免遮挡。
- 新手前 `3次` 进入场景时保留更强的引导，熟练后自动弱化。

### 11.3 场景内容扩展方法

建议正式定义场景扩展的层级模型:

#### 层级一: 事件核心

回答“玩家来这里是为了完成什么?”

- 鸡舍: 投喂、抚摸、拾蛋
- 码头: 听潮、找鱼、抛竿、拉扯
- 果园: 摇树、分拣、压榨、采蜜
- 集市: 看单、交付、议价、变现

#### 层级二: 动态元素

回答“这个场景里什么东西是活的、会变的、值得观察的?”

动态元素不只是角色，也包括:

- 生物行为: 鸡、鱼、蜜蜂、顾客、海鸟
- 自然变化: 风、浪、光影、落叶、水花
- 状态变化: 饥饿/上钩/成熟/售罄/排队
- 节奏事件: 稀有刷新、连续成功奖励、特殊订单

#### 层级三: 静态道具

回答“玩家靠什么进入交互，靠什么理解这个空间的故事?”

静态元素应承担三件事:

1. 作为交互锚点，例如料斗、食槽、鱼竿桶、压榨台、订单板。
2. 作为世界叙事细节，例如鸡窝、渔网架、果筐、摊位招牌。
3. 作为资源流转节点，例如收纳箱、加工台、售卖口、升级台。

建议每个场景都至少配置:

- `1个主交互站点`
- `2-3个辅助交互点`
- `2个纯叙事静态物`
- `1个资源出口或加工出口`

### 11.4 四个场景的内容完善建议

#### A. 暖穗鸡舍

| 维度 | 建议内容 |
|------|------|
| 事件核心 | 养鸡日常：喂食、安抚、拾蛋 |
| 动态元素 | 鸡会巡游、抢食、靠近玩家、被抚摸后开心抖动；鸡蛋可加入“刚下蛋的闪光时刻” |
| 静态元素 | 料斗、食槽、鸡窝、干草堆、水盆、保温灯、蛋篮 |
| 可扩展事件 | 稀有蛋、挑食鸡、怕生鸡、临时缺水、夜里保温不足 |
| 资源出口 | 鸡蛋可直接卖、做拼盘订单、和果园/集市形成联动 |

鸡舍应继续作为最稳定、最治愈、最适合新手理解规则的起始场景。

#### B. 听潮码头

| 维度 | 建议内容 |
|------|------|
| 事件核心 | 听音找位、判断鱼情、抛竿、拉鱼上岸 |
| 动态元素 | `必须补强鱼元素`：加入鱼群影子、近岸小鱼巡游、偶发跃水、不同鱼种的上钩习性；海鸟盘旋、浪花节奏、水面浮标偏移 |
| 静态元素 | 鱼竿桶、鱼饵盒、渔网、装鱼木箱、系船桩、潮位尺、铃铛浮标 |
| 可扩展事件 | 近岸小鱼适合新手，深水大鱼需要更准的拉扯；阴天高价值鱼更易出现；偶尔出现贝壳或漂流物 |
| 资源出口 | 鱼可直接送去市场鱼摊，也可进入“海风拼盘”订单或次日特殊委托 |

码头当前最缺的是“被钓对象的存在感”。玩家不能只对着水花点击，还要看到鱼、猜鱼、追鱼。参考 `DAVE THE DIVER` 和 `Dinkum` 的做法，鱼不仅是奖励结算项，也应该是场景里的主要动态视觉焦点。

建议码头后续细化为三段体验:

1. `观察阶段`: 通过水花、倒影、鱼群方向、海鸟俯冲判断哪里有鱼。
2. `上钩阶段`: 不同鱼种有不同咬钩延迟和挣扎节奏。
3. `收获阶段`: 鱼被拉上来后有短暂展示，强化“我钓到了什么”的满足感。

#### C. 风磨果园

| 维度 | 建议内容 |
|------|------|
| 事件核心 | 摇树收果、分出好果、加工次果、收集蜂蜜 |
| 动态元素 | 树叶摆动、果子坠落轨迹、蜜蜂绕箱、熟果颜色变化、鸟停枝头偷食 |
| 静态元素 | 果树、果筐、压榨桶、蜂箱、梯子、风车、水渠、运输小车 |
| 可扩展事件 | 熟透果更值钱但更易摔坏；雨后蜂蜜减产；风大时更容易摇落果子 |
| 资源出口 | 好果卖鲜果，次果做果酱，蜂蜜可给市场高价摊位或做礼盒订单 |

果园适合作为“加工链”的起点，让玩家第一次感受到不是所有资源都应该直接出售。

#### D. 暮灯集市

| 维度 | 建议内容 |
|------|------|
| 事件核心 | 看订单、找对应摊位、交货、快速出售、换取金币和声望 |
| 动态元素 | 顾客排队、摊主招呼、灯笼摆动、限时订单刷新、议价成功/失败反应 |
| 静态元素 | 订单板、农货摊、鱼摊、手作摊、称重台、包装桌、货架、灯牌 |
| 可扩展事件 | 稀有顾客点名要某类货；夜市时段出现高价收购；声望提高后解锁更高阶订单 |
| 资源出口 | 把鸡舍、码头、果园产出的物资都变成金币、声望和升级材料 |

集市应是全局循环的“兑现层”。如果鸡舍、码头、果园负责产出，集市就负责让资源变成长期成长。

### 11.5 跨场景资源和故事联动

为了避免四个场景彼此孤立，建议加入最基础的资源链:

| 来源场景 | 主要产出 | 去向 |
|------|------|------|
| 鸡舍 | 鸡蛋、好感 | 集市订单、组合料理、稀有蛋收藏 |
| 码头 | 鱼、贝壳、漂流物 | 鱼摊出售、拼盘订单、装饰升级 |
| 果园 | 鲜果、果酱、蜂蜜 | 农货摊、手作摊、节日礼盒 |
| 集市 | 金币、声望、订单推进 | 解锁新装饰、新事件、新稀有产物 |

建议后续加入以下轻故事线:

- 鸡舍主线强调“照料关系”。
- 码头主线强调“观察自然与等待收获”。
- 果园主线强调“分拣与加工判断”。
- 集市主线强调“把一天的劳动变成认可和交换”。

这样四个场景就不只是四个小游戏，而是同一天生活切片的四个面。

### 11.6 其他值得优先迭代的部分

#### A. 声音系统

当前项目非常适合用声音强化空间感和目标感，优先级应该高于继续堆更多 UI。

建议补充:

- 鸡舍: 咕咕声、啄食声、干草落下声、拾蛋轻响
- 码头: 海浪、木板吱呀、海鸟叫、鱼跃水声、浮标提示音
- 果园: 风声、落果声、蜜蜂绕行声、压榨声
- 集市: 人群杂谈、摊主招呼、钱币声、灯笼环境音

码头尤其应采用“先听到，再看到”的设计，让声音本身成为玩法的一部分。

#### B. 长线成长

建议加入不会破坏轻量节奏的长期成长:

- 场景评级: S/A/B 影响额外金币或声望
- 装饰升级: 鸡舍挂件、码头渔具、果园风车、集市灯牌
- 工具升级: 更稳的鱼竿、更大的蛋篮、更快的压榨桶
- 收藏目标: 稀有蛋图鉴、鱼类图鉴、果酱配方、订单成就

#### C. 数据驱动化

当前已经有明确的 `scene.id` 和对象数据结构，后续建议把“场景定义”进一步抽离成数据表:

- `scene-meta.js` 负责场景目标、时长、主题色
- `scene-objects.js` 负责动态元素和静态道具配置
- `scene-events.js` 负责稀有事件、完成条件、奖励

这样未来加新场景时，不需要把逻辑继续堆进单个大文件。

#### D. 新手引导和前几日节奏

建议加入轻教程节奏:

- `Day 1` 只开放鸡舍，建立基本交互模型
- `Day 2` 开码头，引入观察和节奏判断
- `Day 3` 开果园，引入加工分流
- `Day 4` 开集市，引入订单和资源兑现

这种逐日解锁方式和 `Cozy Grove` 的日常推进、`Coral Island` 的生活模拟展开方式更接近，也更适合降低首次上手门槛。

### 11.7 建议版本路线图

| 版本 | 目标 | 重点内容 |
|------|------|------|
| `v0.2` | 先把体验打磨顺 | HUD层级、上下文提示、准星状态、误触容错、统一反馈语言 |
| `v0.3` | 补齐场景生命感 | 码头加入鱼群与鱼种反馈，鸡舍/果园/集市补足动态元素和静态叙事物 |
| `v0.4` | 建立资源闭环 | 场景产出联动市场订单，加入金币/声望/升级反馈 |
| `v0.5` | 建立长期目标 | 图鉴、装饰升级、稀有事件、节日订单、连续游玩奖励 |

### 11.8 外部参考方向

以下项目适合作为这一轮迭代的方向参考:

- `Stardew Valley`：动物照料、好感与资源产出绑定，适合参考鸡舍日常的可持续反馈。
- `DAVE THE DIVER`：采集阶段和经营阶段互相喂养，适合参考码头到集市的双循环。
- `Cozy Grove`：日常任务节奏清晰、场景会随着帮助推进而“活起来”，适合参考轻剧情推进。
- `Coral Island`：把农耕、养殖、钓鱼、社区关系放在统一生活模拟框架里，适合参考多场景统一感。
- `Dinkum`：用天气、季节、鱼虫和建造让每一天都略有变化，适合参考动态环境层。

参考链接:

- https://wiki.stardewvalley.net/Animals
- https://store.steampowered.com/app/1868140/DAVE_THE_DIVER/
- https://store.steampowered.com/app/1458100/Cozy_Grove/
- https://store.steampowered.com/app/1158160/Coral_Island/
- https://store.steampowered.com/app/1062520/Dinkum/

---

## 十二、世界观与界面文案说明

这一部分用于统一 `穗潮湾` 的气质、对外描述方式，以及首页/场景/结算等界面可直接使用的文案内容。

### 12.1 设计说明

`穗潮湾` 不是一个强调冲刺、对抗、扩张的经营游戏，而是一个强调“把一天慢慢做顺”的轻叙事生活模拟原型。

它的核心设计意图是：

- 让玩家在短时游玩里体验一种有节律的日常劳动。
- 让“照料 -> 收获 -> 加工 -> 交换”形成温和但明确的回环。
- 让每个场景既是一个玩法节点，也是湾区生活的一小段切面。
- 让玩家感受到自己不是在“刷任务”，而是在照看一个会回应自己的地方。

这套世界观的关键词建议统一为：

- `潮汐`
- `湾区日常`
- `细小劳动`
- `收成回响`
- `温柔秩序`
- `被看见的付出`

### 12.2 内容说明

#### 世界观定位

`穗潮湾` 是一个靠海的小湾聚落。这里没有宏大的灾难叙事，也没有强烈的冒险主线，生活由四件事组成：

- 早晨照料活物
- 白天观察自然
- 午后整理收成
- 傍晚完成交换

玩家扮演的不是英雄，而是这个小湾里“把日子接起来的人”。  
你会在鸡舍、码头、果园与集市之间来回，把分散的日常劳动串成完整的一天。

#### 四个场景的内容职责

| 场景 | 内容角色 | 情绪关键词 | 玩法感受 |
|------|------|------|------|
| `暖穗鸡舍` | 一天的开始 | 安稳、亲近、照料 | 先照顾，再收获 |
| `听潮码头` | 自然观察与判断 | 专注、聆听、等待 | 先判断，再出手 |
| `风磨果园` | 分类与加工 | 丰盛、判断、整理 | 先分辨，再转化 |
| `暮灯集市` | 回报与流通 | 热闹、兑现、回响 | 先交换，再确认价值 |

#### 叙事节奏

推荐把一天的感受写成：

1. `清晨`：先把小事做好，日子才稳。
2. `白天`：风声、水声、树影都在给出线索。
3. `傍晚`：劳动不只产生资源，也会留下被看见的痕迹。
4. `夜里`：把收成换成金币、声望和明天的期待。

### 12.3 适用场景

这个题材和世界观适合以下使用方向：

- `移动端轻度经营游戏`：单局时间短，适合碎片化体验。
- `治愈向/Cozy 向原型展示`：能快速传达“温柔劳动”的气质。
- `陀螺仪/视角交互展示项目`：定点观察 + 微操作的结构清晰。
- `课程作业 / 毕设原型 / 展会 Demo`：四场景结构完整，易于拆分演示。
- `女性向、生活模拟、轻叙事用户测试`：更容易验证题材吸引力与界面文案风格。

用户游玩场景建议重点面向：

- 通勤途中 3~8 分钟的短时体验
- 睡前放松的低压力游玩
- 想要“有事可做，但不想被高强度挑战追赶”的玩家
- 喜欢种田、钓鱼、经营、收集和轻叙事氛围的用户

### 12.4 首页可用引言

以下文案可直接用于首页首屏：

#### 标题辅助句

- 一个关于照料、观察、收成与交换的小湾
- 在潮水退去以后，把一天慢慢做顺
- 从鸡舍到夜市，把细小劳动变成看得见的回响

#### 首页正文候选

- 潮水退去后，鸡舍、码头、果园和夜市会慢慢亮起来。
- 你在一天之内穿过四段日常，把分散的收成接成完整的生活回环。
- 这里没有惊天动地的传奇，只有被认真照看的风、潮水、果香和灯火。

#### 首页底部短句

- 潮水会退去，日子会留下。
- 今天适合从一捆干草开始。
- 把白天的收成，带回夜里的灯火里。

#### 首页功能概括短语

- 照料 · 观察 · 收获 · 交换
- 轻劳动 · 轻叙事 · 轻经营
- 从晨光到夜市的一日回环

### 12.5 场景界面文案库

以下内容适合用于 `场景进入提示`、`顶部小引言`、`场景卡片副文案`、`结果页小标题`。

#### 暖穗鸡舍

- 进入句：鸡舍总是醒得最早。先把干草放好，再和小鸡们打个招呼。
- 顶部小引言：晨光落进鸡舍时，照料就是一天最稳的开始。
- 结果页短句：今天的安稳，是从第一把干草开始的。

#### 听潮码头

- 进入句：海面把鱼群藏进了浪声里。跟着更近的水花，把这一杆抛准。
- 顶部小引言：在穗潮湾，鱼有时先被听见，才会被看见。
- 结果页短句：每一次起钩，都是对白天风向的一次回答。

#### 风磨果园

- 进入句：风会替树把成熟交下来。你要做的，是分出最甜的一份。
- 顶部小引言：果园教人的不是采摘，而是判断什么该留下、什么该变成香气。
- 结果页短句：好果入筐，余香入酱，都是今天的收成。

#### 暮灯集市

- 进入句：等灯火亮起，白天的劳动才真正有了回声。看看谁在等你的货。
- 顶部小引言：夜市不只交换货物，也交换一天被看见的分量。
- 结果页短句：被带到摊位上的，不只是货物，也是今天的分量。

### 12.6 可直接放在界面里的标签文案

适合用于 HUD、小标题、面板名、信息条：

- `今日要务`
- `湾里近况`
- `今日收成`
- `收成去向`
- `今日订单`
- `摊位行情`
- `夜市回响`
- `明天的准备`
- `风向与潮声`
- `鸡舍安稳度`

### 12.7 结算 / 次日过渡文案

#### 结算页

- 今天做顺了不少事情。
- 白天的收成都有了去处。
- 穗潮湾记住了你今天的劳动。

#### 睡到明天 / 次日开启

- 新的一天又被潮声推了过来。
- 灯火熄了，风还在替你照看果树。
- 明天的收成，会从今天的整理里长出来。

#### 轻提示语气建议

界面里的提示文案整体建议遵守以下语气：

- 多用 `提醒`，少用 `命令`
- 多用 `日常语言`，少用 `系统术语`
- 多写“下一步该做什么”，少写抽象说明
- 成功反馈偏温暖，失败反馈偏轻柔，不用挫败型措辞

例如：

- 推荐：`先去料斗拿一捆干草`
- 推荐：`这边鱼情不太对，换个方向听听`
- 推荐：`等灯火亮起，白天的劳动才真正有了回声`
- 不推荐：`操作失败`
- 不推荐：`目标错误`
- 不推荐：`资源不足，无法执行`

---

## 附录: 完整文件列表

```
/scenes/暖穗鸡舍/
├── index.html              # 入口 (含Three.js CDN)
├── css/
│   └── style.css           # 全部样式 (~500行)
└── js/
    ├── main.js             # 主循环 (~100行)
    ├── config.js           # 常量 (~50行)
    ├── state.js            # 状态 (~80行)
    ├── game-logic.js       # 游戏逻辑 (~300行)
    ├── screens.js          # 屏幕 (~400行)
    ├── ui.js               # UI组件 (~200行)
    ├── input.js            # 输入 (~100行)
    ├── sensors.js          # 陀螺仪 (~100行)
    ├── persistence.js      # 存档 (~100行)
    ├── auth.js             # 账号 (~100行)
    ├── math.js             # 数学 (~80行)
    ├── assets.js           # 资源 (~60行)
    ├── render-bg.js        # 背景 (~80行)
    ├── render-objects.js   # 物品 (~200行)
    └── particles.js        # 粒子 (~150行)
```
